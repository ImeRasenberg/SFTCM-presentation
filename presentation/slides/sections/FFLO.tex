
\begin{frame}{Zeeman Splitting}
	\only<1->{
		\[
		\hspace*{-0.5cm}\mathcal{H} = \sum_{k}\left[h (\hat{n}_{k,\downarrow} - \hat{n}_{k,\uparrow}) + \sum_{\sigma}\epsilon_k \hat{n}_{k,\sigma}\right] 
		+V\sum_{k}	\hat{c}_{k\uparrow}^{\dagger}\hat{c}_{-k\downarrow}^{\dagger}\hat{c}_{k\uparrow}\hat{c}_{-k\downarrow}
		\]
		\vspace{0.2cm}
	}
	\only<2->{
		\[
		\hspace*{-0.5cm}\mathcal{H} = \sum_{k}\left[h (\hat{n}_{k,\downarrow} - \hat{n}_{k,\uparrow}) + \sum_{\sigma}\epsilon_k \hat{n}_{k,\sigma}\right] 
		+V\sum_{k}	\hat{c}_{q+k\uparrow}^{\dagger}\hat{c}_{-k\downarrow}^{\dagger}\hat{c}_{q+k\uparrow}\hat{c}_{-k\downarrow}
		\]
		\vspace{0.2cm}
	}
%		\only<2->{
%		\[
%		\hspace*{-0.5cm}\mathcal{H} = \sum_{k}\left[h (\hat{n}_{k,\downarrow} - \hat{n}_{k,\uparrow}) + \sum_{\sigma}\epsilon_k \hat{n}_{k,\sigma}\right] 
%		+V\sum_{k}	\hat{c}_{\frac{q}{2}+k\uparrow}^{\dagger}\hat{c}_{\frac{q}{2}-k\downarrow}^{\dagger}\hat{c}_{\frac{q}{2}+k\uparrow}\hat{c}_{\frac{q}{2}-k\downarrow}
%		\]
%		\vspace{0.2cm}
%	}
	\only<3->{
		\[
		\begin{split}
			\langle\Delta(x,\tau)\rangle
			&=
			V\langle
			\psi_{q-k \downarrow}(\tau)\psi_{k \uparrow}(\tau)
			\rangle\\
			&=\begin{cases}
				\text{FF:} &
				\langle \Delta_{\mathrm{BCS}}(x,\tau)e^{i q\cdot x}\rangle 
				\\
				\text{LO:} &
				\langle \Delta_{\mathrm{BCS}}(x,\tau)\cos(q\cdot x)\rangle 
			\end{cases}
		\end{split}
		\]
	}
\end{frame}


\begin{frame}{Phase Diagram}
		\only<1->{
			\[
			\hspace{-0.5cm}S_{\mathrm{eff}}
			=
			\int d\tau d^3x
			\left[
			\frac{|\Delta|^2}{V}
			+
			\Psi^\dagger
			\begin{pmatrix}
				i\hbar\omega_n +\epsilon_k - \mu + h & \Delta \\
				\Delta^* & -i\hbar\omega_n +\epsilon_k - \mu - h
			\end{pmatrix}
			\Psi
			\right]
			\]
		}
		\only<2->{
			\[
			S_{\mathrm{eff}} = V_0 \beta \frac{|\Delta|^2}{V} - \frac{1}{2}\mathrm{Tr}\log -\mathbf{G}_0^{-1} - \frac{1}{2}\mathrm{Tr}\log(1-\mathbf{G}_0 \Sigma)
			\]
		}
		\only<3->{
			\[
			\Delta_{\mathrm{BCS}}(x,\tau) \ge \Delta_{\mathrm{BCS}}(x,\tau)\cos(q\cdot x)
			\]
		}
\end{frame}